apiVersion: v1
kind: Pod
metadata:
  name: toleration-delay
spec:
  containers:
  - name: nginx
    image: nginx
  tolerations:
  - key: "dev"
    operator: "Equal"
    value: "true"
    effect: "NoExecute"
    tolerationSeconds: 30
